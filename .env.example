# API Configuration
# Copy this file to .env.local and update with your values

# NEXT_PUBLIC_API_URL: Client-side API endpoint (proxied through Next.js)
# For both dev and production, use /api to call Next.js API routes
NEXT_PUBLIC_API_URL=/api

# NEXT_PUBLIC_WS_URL: Client-side WebSocket endpoint for real-time updates
# Development: ws://localhost:3001/cable
# Production: wss://reporeconnoiter.com/cable
NEXT_PUBLIC_WS_URL=ws://localhost:3001/cable

# REPO_RECONNOITER_API_URL: Server-side backend API endpoint (used by Next.js API routes)
# Development: http://localhost:3001/api/v1
# Production: https://api.reporeconnoiter.com/v1
REPO_RECONNOITER_API_URL=http://localhost:3001/api/v1

# API_KEY: Server-side only API key for backend API
# Never expose this in browser code (no NEXT_PUBLIC_ prefix)
API_KEY=your-api-key-here

# NextAuth Configuration
# NEXTAUTH_SECRET: Random secret for NextAuth JWT encryption
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=your-secret-here

# NEXTAUTH_URL: Canonical URL of your site
# Development: http://localhost:3000
# Production: https://reporeconnoiter.com
NEXTAUTH_URL=http://localhost:3000

# GitHub OAuth Configuration
# Create a GitHub OAuth app at: https://github.com/settings/developers
# Authorization callback URL: http://localhost:3000/api/auth/callback/github (dev)
#                            https://reporeconnoiter.com/api/auth/callback/github (prod)
GITHUB_CLIENT_ID=your-github-oauth-app-id
GITHUB_CLIENT_SECRET=your-github-oauth-app-secret